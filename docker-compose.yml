version: "3.9"

services:
  app:
    build: .
    image: repomind:dev
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - ./:/app:cached
      - /app/node_modules
    working_dir: /app
    command: sh -lc "npm install --no-audit --no-fund && npm run dev"
    environment:
      - NODE_ENV=development
      - MONGODB_URI=mongodb://mongodb:27017/repoinfo
    depends_on:
      - mongodb

  mongodb:
    image: mongo:7-jammy
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=repoinfo

volumes:
  mongodb_data:
